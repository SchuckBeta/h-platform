<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oseasy.initiate.modules.impdata.dao.ImpInfoDao">

	<sql id="impInfoColumns">
		a.id AS "id",
		a.imp_tpye AS "impTpye",
		a.total AS "total",
		a.success AS "success",
		a.fail AS "fail",
		a.is_complete AS "isComplete",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate"
	</sql>

	<sql id="impInfoJoins">
	</sql>

	<select id="get" resultType="ImpInfo">
		SELECT
		<include refid="impInfoColumns" />
		FROM imp_info a
		<include refid="impInfoJoins" />
		WHERE a.id = #{id}
	</select>

	<select id="findList" resultType="ImpInfo">
		SELECT
		<include refid="impInfoColumns" />
		FROM imp_info a
		<include refid="impInfoJoins" />
		<where>

		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>

	<select id="findAllList" resultType="ImpInfo">
		SELECT
		<include refid="impInfoColumns" />
		FROM imp_info a
		<include refid="impInfoJoins" />
		<where>

		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>

	<insert id="insert">
		INSERT INTO imp_info(
		id,
		imp_tpye,
		total,
		success,
		fail,
		is_complete,
		create_by,
		create_date,
		update_by,
		update_date
		) VALUES (
		#{id},
		#{impTpye},
		#{total},
		#{success},
		#{fail},
		#{isComplete},
		#{createBy.id},
		#{createDate},
		#{updateBy.id},
		#{updateDate}
		)
	</insert>

	<update id="update">
		UPDATE imp_info SET
		imp_tpye = #{impTpye},
		total = #{total},
		success = #{success},
		fail = #{fail},
		is_complete = #{isComplete},
		update_by = #{updateBy.id},
		update_date = #{updateDate}
		WHERE id = #{id}
	</update>

	<update id="delete">
		DELETE FROM imp_info
		WHERE id = #{id}
	</update>
	<select id="getList" resultType="java.util.HashMap"
		parameterType="java.util.Map">
		select t.id,
		t.imp_tpye,t.total,t.success,t.fail,t.is_complete,date_format(t.create_date,'%Y-%m-%d %H:%i:%s') create_date from imp_info t
		where t.create_by=#{userid} order by t.create_date desc limit
		#{offset},#{pageSize}
	</select>
	<select id="getListCount" resultType="java.lang.Integer"
		parameterType="java.util.Map">
		select count(1) from imp_info t
		where t.create_by=#{userid}
	</select>
</mapper>