<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oseasy.initiate.modules.course.dao.CourseAttachmentDao">
    
	<sql id="courseAttachmentColumns">
		a.id AS "id",
		a.course_id AS "courseId",
		a.url AS "url",
		a.name AS "name",
		a.size AS "size",
		a.suffix AS "suffix",
		a.update_date AS "updateDate",
		a.update_by AS "updateBy.id",
		a.create_date AS "createDate",
		a.create_by AS "createBy.id",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="courseAttachmentJoins">
	</sql>

	<select id="getByCourseId" parameterType="java.lang.String" resultType="CourseAttachment">
		SELECT
		<include refid="courseAttachmentColumns"/>
		FROM course_attachment a
		WHERE a.course_id = #{courseId}
	</select>

	<delete id="deleteByCourseId" parameterType="java.lang.String">
		delete from course_attachment  where course_id = #{courseId}
	</delete>

    
	<select id="get" resultType="CourseAttachment">
		SELECT 
			<include refid="courseAttachmentColumns"/>
		FROM course_attachment a
		<include refid="courseAttachmentJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CourseAttachment">
		SELECT 
			<include refid="courseAttachmentColumns"/>
		FROM course_attachment a
		<include refid="courseAttachmentJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CourseAttachment">
		SELECT 
			<include refid="courseAttachmentColumns"/>
		FROM course_attachment a
		<include refid="courseAttachmentJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO course_attachment(
			id,
			course_id,
			url,
			name,
			size,
			suffix,
			update_date,
			update_by,
			create_date,
			create_by,
			del_flag
		) VALUES (
			#{id},
			#{courseId},
			#{url},
			#{name},
			#{size},
			#{suffix},
			#{updateDate},
			#{updateBy.id},
			#{createDate},
			#{createBy.id},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE course_attachment SET 	
			course_id = #{courseId},
			url = #{url},
			name = #{name},
			size = #{size},
			suffix = #{suffix},
			update_date = #{updateDate},
			update_by = #{updateBy.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE course_attachment SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>